# Filter Script Presets

This directory contains filter script presets for MeshPrep.

## Structure

- `*.json` / `*.yaml` — User and built-in presets
- `community/` — Community-contributed presets

## Creating Presets

See `docs/functional_spec.md` for the filter script format specification.
