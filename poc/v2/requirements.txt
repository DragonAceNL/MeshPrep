# MeshPrep POC v2 - Real Mesh Operations
# These are actual dependencies, not mocked

# ============================================================
# PYTHON VERSION REQUIREMENT
# ============================================================
# Python 3.11 or 3.12 is REQUIRED for pre-built wheels.
# Python 3.13+ does not have pre-built pymeshfix wheels yet.
#
# To check your version: python --version
# To install Python 3.12: https://www.python.org/downloads/
# ============================================================

# Core mesh processing
trimesh>=4.0.0
numpy>=1.24.0
networkx>=3.0  # Required by trimesh for some operations

# Mesh repair (requires Python 3.11 or 3.12 for pre-built wheels)
pymeshfix>=0.16.0

# Surface reconstruction for extreme fragmentation
# Open3D is MIT licensed (free and open source)
# Required for: open3d_screened_poisson, open3d_ball_pivoting, open3d_alpha_shape
# These are essential for fixing 1000+ body fragmented meshes
open3d>=0.18.0

# Spatial operations (for Hausdorff distance, morphological reconstruction)
scipy>=1.10.0

# Image processing for morphological voxel reconstruction
scikit-image>=0.21.0

# Optional: better STL loading and CTM support
meshio>=5.0.0

# Testing and benchmarking
pytest>=7.0.0
tqdm>=4.65.0

# Reporting
tabulate>=0.9.0
